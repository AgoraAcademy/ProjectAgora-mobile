import index from '../pages/index/model';// index 页面的model
import authorize from '../pages/authorize/model'
import booking from '../pages/booking/model'
import home from '../pages/home/model'

// 这里记得export的是数组，不是对象
export default [
    index,
    authorize,
    booking,
    home
]